server:
  forward-headers-strategy: framework

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  jpa:
    show-sql: false

logging:
  level:
    root: INFO
    org.springframework.security.oauth2: DEBUG
    com.cbp7.auth: DEBUG
